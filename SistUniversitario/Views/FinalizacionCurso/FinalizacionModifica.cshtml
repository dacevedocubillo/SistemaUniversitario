@model SistUniversitario.Models.saRegistro_ID_Result

@{
    ViewBag.Title = "FinalizacionModifica";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Finalizacion Curso</h2>

<form id="frmFinalizacion" method="post">
    @Html.AntiForgeryToken()

    <div class="form-group">
        <label>Estudiante :</label>
        <select name="idestudiante" id="idestudiante" class="form-control" disabled>
            <option value="">Seleccione un estudiante</option>
            @{

                foreach (SistUniversitario.Models.RetornaEstudiante_Result estudiante in this.ViewBag.ListaEstudiante)
                {
                    bool esIdEstudiante = estudiante.id_estudiante == Model.idestudiante;

                    if (esIdEstudiante)
                    {
                        <option value="@estudiante.id_estudiante" selected>@estudiante.nombre</option>
                    }
                    else
                    {
                        <option value="@estudiante.id_estudiante">@estudiante.nombre</option>
                    }

                }
            }
        </select>
    </div>

    <div class="form-group">
        <label>Cursos :</label>
        <select name="id_curso" id="id_curso" class="form-control" disabled>
            <option value="">Seleccione un Curso</option>
            @{

                foreach (SistUniversitario.Models.RetornaCurso_Result curso in this.ViewBag.ListaCursos)
                {
                    bool esIdcurso = curso.idCurso == Model.id_curso;

                    if (esIdcurso)
                    {
                        <option value="@curso.idCurso" selected>@curso.nombreCurso</option>
                    }

                    else
                    {
                        <option value="@curso.idCurso">@curso.nombreCurso</option>
                    }

                }
            }
        </select>
    </div>

    <div class="form-group">
        <label>Nota:</label>
        <input type="text" name="notafinal" id="notafinal" class="form-control" value="@Model.notafinal" readonly />
    </div>

    <div class="form-group">
        <label>Estado :</label>
        <input class="form-control" value="@(Model.notafinal >= 7 ? "Aprobado" : "Reprobado")" readonly />
        <small>El estado se calcula automáticamente: aprobado desde 7.0.</small>
    </div>

    <div class="form-group">
        <input type="submit" value="Guardar" id="btnAceptar" />
    </div>
</form>

@section scripts{
    @* *****No es necesario incluir jquery porque
        ya el layout lo incluye*@

}
